version: 0.0
Resources:
  - TargetService:
      Type: AWS::EKS::Kubernetes
      Properties:
        ClusterName: braintask
        Namespace: default
        ManifestFiles:
          - deployment.yaml
          - service.yaml
